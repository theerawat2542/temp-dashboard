import React from "react";
import { ProdLineI } from "./MainLayoutNav";

type Props = {
  prodList: ProdLineI[];
};

const ProdLineGroup = ({ prodList }: Props) => {
  return <button></button>;
};

export default ProdLineGroup;
